name: Upload release assets safely
description: Upload release assets idempotently with verification and bounded retries.

inputs:
  asset-paths:
    description: Newline-delimited release asset paths selected before attestation.
    required: false
  asset-glob:
    description: Legacy caller-owned shell glob used when asset-paths is empty.
    required: false
  github-token:
    description: Token with contents write access to the target repository.
    required: true
  tag:
    description: Existing draft release tag.
    required: true
  working-directory:
    description: Directory from which to expand the asset glob.
    required: false
    default: .

runs:
  using: composite
  steps:
    - name: Upload and verify release assets
      shell: bash
      env:
        ASSET_GLOB: ${{ inputs.asset-glob }}
        ASSET_PATHS: ${{ inputs.asset-paths }}
        GH_TOKEN: ${{ inputs.github-token }}
        TAG: ${{ inputs.tag }}
        WORKING_DIRECTORY: ${{ inputs.working-directory }}
      run: |-
        if [[ -z "$ASSET_PATHS" ]]; then
          cd "$WORKING_DIRECTORY"
        fi
        "$GITHUB_ACTION_PATH/upload.sh"
