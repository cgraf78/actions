name: Verify consumer synchronization
description: Verify that Actions refs, release scripts, and any generated installer come from one locked commit.

runs:
  using: composite
  steps:
    - name: Verify consumer lock and derived files
      shell: bash
      env:
        ACTION_REF: ${{ github.action_ref }}
      run: "$GITHUB_ACTION_PATH/verify.sh"
